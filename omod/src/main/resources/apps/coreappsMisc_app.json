[
    {
        "id": "${project.parent.groupId}.${project.parent.artifactId}.coreappsMiscApp",
        "description": "Contains other miscellaneous extension points",
        "extensionPoints": [
            {
                "id": "${project.parent.artifactId}.patientViewed",
                "description": "Provides a hook into the patient viewed interceptor so that an event gets published that a patient has been viewed, the page name, provider and patient id or uuid request parameter name have to be specified as extension params"
            }
        ]
    }
]