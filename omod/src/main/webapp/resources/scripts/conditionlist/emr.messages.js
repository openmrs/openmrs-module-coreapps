jQuery(function () {
    if (typeof emr !== "undefined") {
        emr.loadMessages([
            "conditionui.updateCondition.success",
            "conditionui.updateCondition.error",
        ]);
    }
})